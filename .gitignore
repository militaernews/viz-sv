/node_modules
/build
# The whole directory is regenerated by `svelte-kit sync` / `vite build` -
# nothing under it should ever be committed (this previously wasn't ignored,
# and a full production build ended up tracked in history).
.svelte-kit

# Env
.env
.env.*
!.env.example
.vercel
.env*
